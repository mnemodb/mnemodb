# @mnemodb/cli

`mnemo` — the MnemoDB command line: `init`, `list`, `show`, `doctor`,
`compact` (dry-run vacuum), `migrate` (CLAUDE.md/AGENTS.md and ADR importers).

Docs and spec: https://github.com/mnemodb/mnemodb
