# @mnemodb/core

Zero-dependency TypeScript implementation of the MnemoDB agent memory format:
parse, byte-stable serialize, index, resolve, lifecycle (TTL/review), CRDT
merge, compaction, migration importers, and store validation (doctor).

Docs and spec: https://github.com/mnemodb/mnemodb
