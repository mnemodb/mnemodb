{
  "name": "mnemodb-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/cli packages/mcp",
    "test": "npm run build && node --test \"packages/core/test/*.test.js\" \"packages/mcp/test/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
